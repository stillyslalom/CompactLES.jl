name: CI

on:
  push:
    branches:
      - main
    tags: ['*']
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ startsWith(github.ref, 'refs/pull/') }}

jobs:
  test:
    name: Julia ${{ matrix.version }} - serial
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      actions: write
      contents: read
    strategy:
      fail-fast: false
      matrix:
        version:
          - '1.10'
          - '1'
          - 'pre'
    steps:
      - uses: actions/checkout@v7
      - uses: julia-actions/setup-julia@v3
        with:
          version: ${{ matrix.version }}
          arch: x64
      - uses: julia-actions/cache@v3
      - uses: julia-actions/julia-buildpkg@v1
      - uses: julia-actions/julia-runtest@v1

  numerics:
    name: Convergence and MPI
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      actions: write
      contents: read
    steps:
      - uses: actions/checkout@v7
      - uses: julia-actions/setup-julia@v3
        with:
          version: '1'
          arch: x64
      - uses: julia-actions/cache@v3
      - uses: julia-actions/julia-buildpkg@v1
      # Neither step below runs with `--check-bounds=yes`. That option gives
      # every package in the tree a second precompile cache, which this job
      # would build from scratch before its first test, and it slows all
      # fourteen processes the distributed step launches. The serial suite is
      # already bounds-checked on three Julia versions by julia-runtest, and an
      # index error in the distributed paths moves a convergence order or an
      # interface error away from machine zero, so it does not pass unseen.
      - name: Run convergence guards
        run: julia --project=. --color=yes test/convergence.jl
      # The runner has four vCPUs. The bundled MPICH (ch4) waits in a
      # collective by spinning with sched_yield between polls, which under
      # Linux CFS does not hand the core to a runnable sibling, so at np = 8
      # every rank blocked in a collective takes a core from a rank still
      # computing or compiling. Measured: the per-rank compile of the suite
      # goes from 181 s at np = 2 to 628 s at np = 8, and a timestep from
      # ~0.04 s to ~7 s, with the step-bound phases (distributed refinement,
      # callback consistency, staged device exchange, sync) 100–250x slower.
      # The full suite at np = 8 was 29 minutes of the job.
      #
      # So the full suite runs at np = 2 and 4, the largest counts the runner
      # holds without oversubscription, and np = 8 runs the phases where
      # eight ranks reach something four do not. Most tests put all ranks on
      # one dimension of SPLITN = 72 points via `splitdims`, and at np = 8
      # that is 9 points per rank, the C8 filter's minimum block and the
      # smallest the reduced-interface solves, the transfer operators, the
      # fold butterfly and the positivity repair ever see: those phases run.
      # Two grids change shape outright: the corner-halo check's default
      # Dims_create grid (2x2x2 at np = 8, 2x2x1 at np = 4) and the tiled
      # fine level's _amr_dims grid (4x2 against 2x2). The slicing phase
      # derives its over-coarse stride from np. The remaining phases (the
      # timestepping runs, the writers, the checkpoint, the sensor
      # comparisons) count ranks or change block sizes only, and they are
      # the step-bound ones. `-t 1` because a rank must not also fan out
      # over threads it does not have.
      - name: Run distributed suite at 2 and 4 ranks, rank-shape phases at 8
        shell: julia --project=. --color=yes {0}
        run: |
          using MPI, Printf
          eight = join(("periodic C6", "pentadiagonal C10", "closed C6",
                        "device line solves", "tiled refinement",
                        "AMR transfer pair", "halo consistency",
                        "off-rank folds", "freestream", "positivity floor",
                        "slicing"), ',')
          MPI.mpiexec() do mpiexec
              julia = Base.julia_cmd()
              for (np, args) in ((2, String[]), (4, String[]),
                                 (8, ["phases=" * eight]))
                  t = @elapsed run(`$mpiexec -n $np $julia --project=. -t 1 test/mpi_tests.jl $args`)
                  @printf("=== np = %d finished in %.1f s ===\n", np, t)
              end
          end

  docs:
    name: Documentation
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      actions: write
      contents: write
      statuses: write
    steps:
      - uses: actions/checkout@v7
      - uses: julia-actions/setup-julia@v3
        with:
          version: '1'
          arch: x64
      - uses: julia-actions/cache@v3
      - name: Configure documentation environment
        shell: julia --project=docs --color=yes {0}
        run: |
          using Pkg
          Pkg.develop(PackageSpec(path=pwd()))
          Pkg.instantiate()
      - uses: julia-actions/julia-docdeploy@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DOCUMENTER_KEY: ${{ secrets.DOCUMENTER_KEY }}
